<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>FC United – Football Club and Sport Team</title>
    <style>
        @@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

        :root {
            --primary-color: #1e3c72;
            --secondary-color: #e9491b;
            --text-color: #333333;
            --bg-color: #f7f7f7;
        }

        *, *::before, *::after {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Montserrat', sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: var(--bg-color);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        /* Header styles */
        .site-header {
            background-color: var(--primary-color);
            padding: 10px 0;
            position: absolute;
            width:115.8em;
            z-index: 900;
            transition: background-color 0.3s ease;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .site-logo img {
            max-height: 60px;
            transition: transform 0.3s ease;
        }

        .site-logo img:hover {
            transform: scale(1.05);
        }

        .main-navigation ul {
            list-style: none;
            display: flex;
            margin: 0;
            padding: 0;
        }

        .main-navigation li {
            margin-left: 20px;
        }

        .main-navigation a {
            color: #ffffff;
            text-decoration: none;
            font-weight: 600;
            text-transform: uppercase;
            transition: color 0.3s ease;
        }

        .main-navigation a:hover {
            color: var(--secondary-color);
        }

        /* Hero section styles */
        .hero-section {
            background-image: url('https://joomsport.fc-united.axiomthemes.com/wp-content/uploads/2019/02/anim-bg-copyright.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: #ffffff;
            position: relative;
            overflow: hidden;
        }

        .hero-content {
            max-width: 800px;
            padding: 0 20px;
            position: relative;
            z-index: 2;
        }

        .hero-title {
            font-size: 4em;
            margin-bottom: 20px;
            font-weight: 700;
            opacity: 0;
            transform: translateY(50px);
            animation: fadeInUp 1s forwards 0.5s;
        }

        .hero-subtitle {
            font-size: 1.5em;
            margin-bottom: 30px;
            opacity: 0;
            transform: translateY(50px);
            animation: fadeInUp 1s forwards 0.7s;
        }

        .cta-button {
            display: inline-block;
            background-color: var(--secondary-color);
            color: #ffffff;
            padding: 12px 30px;
            text-decoration: none;
            font-weight: 600;
            text-transform: uppercase;
            border-radius: 3px;
            transition: background-color 0.3s ease, transform 0.3s ease;
            opacity: 0;
            transform: translateY(50px);
            animation: fadeInUp 1s forwards 0.9s;
        }

        .cta-button:hover {
            background-color: #d13d0f;
            transform: translateY(-3px);
        }

        /* Featured sections */
        .featured-section {
            padding: 80px 0;
            text-align: center;
            background-color: #ffffff;
        }

        .section-title {
            font-size: 2.5em;
            color: var(--primary-color);
            margin-bottom: 40px;
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 50px;
            height: 3px;
            background-color: var(--secondary-color);
            margin: 20px auto 0;
        }

        .featured-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .featured-item {
            background-color: var(--bg-color);
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .featured-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        }

        .featured-item img {
            max-width: 100%;
            height: auto;
            margin-bottom: 20px;
            border-radius: 5px;
        }

        .featured-item h3 {
            color: var(--primary-color);
            margin-bottom: 10px;
        }

        /* Next Match Section */
        .next-match-section {
            background-color: var(--primary-color);
            color: #ffffff;
            padding: 60px 0;
            text-align: center;
        }

        .next-match-content {
            display: flex;
            justify-content: space-around;
            align-items: center;
            margin-top: 40px;
        }

        .team-logo {
            width: 100px;
            height: 100px;
            background-color: #ffffff;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .team-logo img {
            max-width: 80%;
            max-height: 80%;
        }

        .match-details {
            font-size: 1.2em;
        }

        /* Stats Section */
        .stats-section {
            background-color: #f0f0f0;
            padding: 60px 0;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 3em;
            font-weight: 700;
            color: var(--secondary-color);
        }

        .stat-label {
            font-size: 1.2em;
            color: var(--primary-color);
        }

        /* Team Section */
        .team-section {
            padding: 80px 0;
            background-color: #ffffff;
        }

        .team-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 30px;
        }

        .team-member {
            text-align: center;
        }

        .team-member img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 15px;
        }

        .team-member h3 {
            margin: 0;
            color: var(--primary-color);
        }

        .team-member p {
            margin: 5px 0 0;
            color: var(--secondary-color);
        }

        /* Sponsors Section */
        .sponsors-section {
            padding: 60px 0;
            background-color: #f0f0f0;
        }

        .sponsors-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 30px;
            align-items: center;
        }

        .sponsor-logo {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100px;
        }

        .sponsor-logo img {
            max-width: 100%;
            max-height: 80px;
            filter: grayscale(100%);
            transition: filter 0.3s ease;
        }

        .sponsor-logo:hover img {
            filter: grayscale(0%);
        }

        /* Footer styles */
        .site-footer {
            background-color: var(--primary-color);
            color: #ffffff;
            padding: 60px 0 30px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 30px;
        }

        .footer-widget h4 {
            color: #ffffff;
            margin-bottom: 20px;
            position: relative;
        }

        .footer-widget h4::after {
            content: '';
            display: block;
            width: 30px;
            height: 2px;
            background-color: var(--secondary-color);
            margin-top: 10px;
        }

        .footer-widget ul {
            list-style: none;
            padding: 0;
        }

        .footer-widget li {
            margin-bottom: 10px;
        }

        .footer-widget a {
            color: #ffffff;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-widget a:hover {
            color: var(--secondary-color);
        }

        .copyright {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid rgba(255,255,255,0.1);
        }

        /* Animations */
        @@keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in {
            opacity: 0;
            transform: translateY(50px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .fade-in.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Responsive design */
        @@media (max-width: 768px) {
            .hero-title {
                font-size: 3em;
            }

            .hero-subtitle {
                font-size: 1.2em;
            }

            .featured-grid, .stats-grid, .team-grid, .sponsors-grid {
                grid-template-columns: 1fr;
            }

            .footer-content {
                grid-template-columns: 1fr;
            }

            .next-match-content {
                flex-direction: column;
            }

            .team-logo {
                margin: 20px 0;
            }
        }
    </style>
</head>
<body>
    <header class="site-header">
        <div class="container">
            <div class="header-content">
                <div class="site-logo">
                    <img src="~/Images/football.png" alt="FC United Logo">
                </div>
                <nav class="main-navigation">
                    <ul>
                        <li><a href="#">Home</a></li>
                        <li><a href="#">About</a></li>
                        <li><a href="#">Team</a></li>
                        <li><a href="#">Matches</a></li>
                        <li><a href="#">News</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <main>
        <section class="hero-section">
            <div class="hero-content">
                <h1 class="hero-title">FC United</h1>
                <p class="hero-subtitle">Your local football club with a global spirit</p>
                <a href="#" class="cta-button">Join the Club</a>
            </div>
        </section>

        <section class="next-match-section">
            <div class="container">
                <h2 class="section-title">Next Match</h2>
                <div class="next-match-content">
                    <div class="team-logo">
                        <img src="~/Images/Logo/Man_Utd_FC_.svg.png" alt="Manchester United">
                    </div>
                    <div class="match-details">
                        <p>FC United vs. Rival FC</p>
                        <p>September 15, 2024 - 19:00</p>
                        <p>City Stadium</p>
                    </div>
                    <div class="team-logo">
                        <img src="~/Images/Logo/Logo_Real_Madrid.svg.png" alt="Real Marid ">
                    </div>
                </div>
            </div>
        </section>

        <section class="featured-section">
            <div class="container">
                <h2 class="section-title fade-in">Latest News</h2>
                <div class="featured-grid">
                    <div class="featured-item fade-in">
                        <img src="/api/placeholder/400/300" alt="News 1">
                        <h3>Victory in the League Cup</h3>
                        <p>FC United triumphs in a thrilling match against rivals.</p>
                    </div>
                    <div class="featured-item fade-in">
                        <img src="/api/placeholder/400/300" alt="News 2">
                        <h3>New Player Signing</h3>
                        <p>Exciting new talent joins our squad for the upcoming season.</p>
                    </div>
                    <div class="featured-item fade-in">
                        <img src="/api/placeholder/400/300" alt="News 3">
                        <h3>Community Outreach Program</h3>
                        <p>FC United launches initiative to support local youth sports.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="stats-section">
            <div class="container">
                <h2 class="section-title fade-in">Club Statistics</h2>
                <div class="stats-grid">
                    <div class="stat-item fade-in">
                        <div class="stat-number">25</div>
                        <div class="stat-label">Trophies Won</div>
                    </div>
                    <div class="stat-item fade-in">
                        <div class="stat-number">100</div>
                        <div class="stat-label">Matches Played</div>
                    </div>
                    <div class="stat-item fade-in">
                        <div class="stat-number">50,000</div>
                        <div class="stat-label">Fans Worldwide</div>
                    </div>
                    <div class="stat-item fade-in">
                        <div class="stat-number">30</div>
                        <div class="stat-label">Team Players</div>
                    </div>
                </div>
            </div>
        </section>

        <section class="team-section">
            <div class="container">
                <h2 class="section-title fade-in">Our Team</h2>
                <div class="team-grid">
                    <div class="team-member fade-in">
                        <img src="~/Images/Welcomback-Ronaldo.jpg"q alt="Player 1">
                        <h3>John Doe</h3>
                        <p>Forward</p>
                    </div>
                    <div class="team-member fade-in">
                        <img src="~/Images/Ronaldo.jpg" alt="Player 2">
                        <h3>Jane Smith</h3>
                        <p>Midfielder</p>
                    </div>
                    <div class="team-member fade-in">
                        <img src="~/Images/Ronaldo.jpg" alt="Player 3">
                        <h3>Mike Johnson</h3>
                        <p>Defender</p>
                    </div>
                    <div class="team-member fade-in">
                        <img src="~/Images/Ronaldo.jpg" alt="Player 4">
                        <h3>Sarah Williams</h3>
                        <p>Goalkeeper</p>
                    </div>
                </div>
                <div class="text-center" style="margin-top: 40px;">
                    <a href="#" class="cta-button">View Full Team</a>
                </div>
            </div>
        </section>

        <section class="sponsors-section">
            <div class="container">
                <h2 class="section-title fade-in">Our Sponsors</h2>
                <div class="sponsors-grid">
                    <div class="sponsor-logo fade-in">
                        <img src="/api/placeholder/150/80" alt="Sponsor 1">
                    </div>
                    <div class="sponsor-logo fade-in">
                        <img src="/api/placeholder/150/80" alt="Sponsor 2">
                    </div>
                    <div class="sponsor-logo fade-in">
                        <img src="/api/placeholder/150/80" alt="Sponsor 3">
                    </div>
                    <div class="sponsor-logo fade-in">
                        <img src="/api/placeholder/150/80" alt="Sponsor 4">
                    </div>
                    <div class="sponsor-logo fade-in">
                        <img src="/api/placeholder/150/80" alt="Sponsor 5">
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="site-footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-widget">
                    <h4>About FC United</h4>
                    <p>FC United is a community-focused football club dedicated to excellence both on and off the pitch. Join us in our journey to become a leading force in football.</p>
                </div>
                <div class="footer-widget">
                    <h4>Quick Links</h4>
                    <ul>
                        <li><a href="#">Home</a></li>
                        <li><a href="#">Team</a></li>
                        <li><a href="#">Matches</a></li>
                        <li><a href="#">News</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-widget">
                    <h4>Contact Us</h4>
                    <p>123 Stadium Road<br>Football City, FC 12345<br>Phone: (123) 456-7890<br>Email: info@fcunited.com</p>
                </div>
                <div class="footer-widget">
                    <h4>Follow Us</h4>
                    <ul>
                        <li><a href="#"><i class="fab fa-facebook-f"></i> Facebook</a></li>
                        <li><a href="#"><i class="fab fa-twitter"></i> Twitter</a></li>
                        <li><a href="#"><i class="fab fa-instagram"></i> Instagram</a></li>
                        <li><a href="#"><i class="fab fa-youtube"></i> YouTube</a></li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; 2024 FC United. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // Scroll animation for header
        window.addEventListener('scroll', function () {
            var header = document.querySelector('.site-header');
            if (window.scrollY > 50) {
                header.style.backgroundColor = 'rgba(30, 60, 114, 0.9)';
            } else {
                header.style.backgroundColor = 'var(--primary-color)';
            }
        });

        // Fade-in animation for sections
        function fadeInOnScroll() {
            var elements = document.querySelectorAll('.fade-in');
            elements.forEach(function (element) {
                var elementTop = element.getBoundingClientRect().top;
                var elementVisible = 150;
                if (elementTop < window.innerHeight - elementVisible) {
                    element.classList.add('active');
                }
            });
        }

        window.addEventListener('scroll', fadeInOnScroll);
        window.addEventListener('load', fadeInOnScroll);
    </script>
</body>
</html>
